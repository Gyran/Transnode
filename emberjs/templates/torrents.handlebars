<thead>
<tr>
    {{#each controller.controllers.torrentColumns.columns}}
        <th>
            {{name}}
        </th>
    {{/each}}
</tr>
</thead>
<tbody>
    {{#each view.content}}
        {{view view.torrentView contentBinding='this'}}
    {{/each}}
</tbody>
